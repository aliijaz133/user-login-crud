import { FormVisibilityDirective } from './form-visibility.directive';

describe('FormVisibilityDirective', () => {
  it('should create an instance', () => {
    const directive = new FormVisibilityDirective();
    expect(directive).toBeTruthy();
  });
});
